@extends('layouts.app')

@section('content')
<div class="container">
    <div class="card">
        <div class="card-body">
            <div style="display: flex; justify-content: space-between;">
                <h4 class="card-title">
                    @lang('crud.all_nominees.index_title')
                </h4>
            </div>

            <div class="searchbar mt-4 mb-5">
                <div class="row">
                    <div class="col-md-6">
                        <form>
                            <div class="input-group">
                                <input
                                    id="indexSearch"
                                    type="text"
                                    name="search"
                                    placeholder="{{ __('crud.common.search') }}"
                                    value="{{ $search ?? '' }}"
                                    class="form-control"
                                    autocomplete="off"
                                />
                                <div class="input-group-append">
                                    <button
                                        type="submit"
                                        class="btn btn-primary"
                                    >
                                        <i class="icon ion-md-search"></i>
                                    </button>
                                </div>
                            </div>
                        </form>
                    </div>
                    <div class="col-md-6 text-right">
                        @can('create', App\Models\Nominees::class)
                        <a
                            href="{{ route('all-nominees.create') }}"
                            class="btn btn-primary"
                        >
                            <i class="icon ion-md-add"></i>
                            @lang('crud.common.create')
                        </a>
                        @endcan
                    </div>
                </div>
            </div>

            <div class="table-responsive">
                <table class="table table-borderless table-hover">
                    <thead>
                        <tr>
                            <th class="text-left">
                                @lang('crud.all_nominees.inputs.account_no')
                            </th>
                            <th class="text-left">
                                @lang('crud.all_nominees.inputs.name')
                            </th>
                            <th class="text-left">
                                @lang('crud.all_nominees.inputs.phone')
                            </th>
                            <th class="text-left">
                                @lang('crud.all_nominees.inputs.relation')
                            </th>
                            <th class="text-left">
                                @lang('crud.all_nominees.inputs.address')
                            </th>
                            <th class="text-right">
                                @lang('crud.all_nominees.inputs.percentage')
                            </th>
                            <th class="text-left">
                                @lang('crud.all_nominees.inputs.user_id')
                            </th>
                            <th class="text-center">
                                @lang('crud.common.actions')
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @forelse($allNominees as $nominees)
                        <tr>
                            <td>{{ $nominees->account_no ?? '-' }}</td>
                            <td>{{ $nominees->name ?? '-' }}</td>
                            <td>{{ $nominees->phone ?? '-' }}</td>
                            <td>{{ $nominees->relation ?? '-' }}</td>
                            <td>{{ $nominees->address ?? '-' }}</td>
                            <td>{{ $nominees->percentage ?? '-' }}</td>
                            <td>
                                {{ optional($nominees->user)->name ?? '-' }}
                            </td>
                            <td class="text-center" style="width: 134px;">
                                <div
                                    role="group"
                                    aria-label="Row Actions"
                                    class="btn-group"
                                >
                                    @can('update', $nominees)
                                    <a
                                        href="{{ route('all-nominees.edit', $nominees) }}"
                                    >
                                        <button
                                            type="button"
                                            class="btn btn-light"
                                        >
                                            <i class="icon ion-md-create"></i>
                                        </button>
                                    </a>
                                    @endcan @can('view', $nominees)
                                    <a
                                        href="{{ route('all-nominees.show', $nominees) }}"
                                    >
                                        <button
                                            type="button"
                                            class="btn btn-light"
                                        >
                                            <i class="icon ion-md-eye"></i>
                                        </button>
                                    </a>
                                    @endcan @can('delete', $nominees)
                                    <form
                                        action="{{ route('all-nominees.destroy', $nominees) }}"
                                        method="POST"
                                        onsubmit="return confirm('{{ __('crud.common.are_you_sure') }}')"
                                    >
                                        @csrf @method('DELETE')
                                        <button
                                            type="submit"
                                            class="btn btn-light text-danger"
                                        >
                                            <i class="icon ion-md-trash"></i>
                                        </button>
                                    </form>
                                    @endcan
                                </div>
                            </td>
                        </tr>
                        @empty
                        <tr>
                            <td colspan="8">
                                @lang('crud.common.no_items_found')
                            </td>
                        </tr>
                        @endforelse
                    </tbody>
                    <tfoot>
                        <tr>
                            <td colspan="8">{!! $allNominees->render() !!}</td>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>
@endsection
